# LLM Cache Gateway

An OpenAI-compatible caching proxy for LLM APIs, with measurement of when
semantic caching returns incorrect answers.

## Status
In development.

## Setup
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```
